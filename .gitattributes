# Shell scripts must stay LF — CRLF breaks bash (incl. Git Bash on Windows).
*.sh text eol=lf
# PowerShell is happy with CRLF on every platform; keep Windows-native endings.
*.ps1 text eol=crlf
*.md text
*.png binary
